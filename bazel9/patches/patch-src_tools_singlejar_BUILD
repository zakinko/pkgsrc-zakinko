$NetBSD$

Link singlejar against libm.

The maths functions are in a separate library on every BSD.  FreeBSD and
OpenBSD already say so here.

--- src/tools/singlejar/BUILD.orig
+++ src/tools/singlejar/BUILD
@@ -87,6 +87,7 @@
     ],
     linkopts = select({
         "//src/conditions:freebsd": ["-lm"],
+        "//src/conditions:netbsd": ["-lm"],
         "//src/conditions:openbsd": ["-lm"],
         "//conditions:default": [],
     }),
@@ -107,6 +108,7 @@
     ],
     linkopts = select({
         "//src/conditions:freebsd": ["-lm"],
+        "//src/conditions:netbsd": ["-lm"],
         "//src/conditions:openbsd": ["-lm"],
         "//conditions:default": [],
     }),
