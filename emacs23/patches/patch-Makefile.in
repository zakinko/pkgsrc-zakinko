$NetBSD$

Three things in the install rules, the same three as editors/emacs22, and
one more for the versioned names.

Drop "info" from INFO_FILES: info/info is the Info reader's own manual, which
this package does not install.  Drop the loop that registers the installed
manuals in the dir file, because pkgsrc runs install-info itself from the
PLIST and the entries would go in twice.  Drop the chmod a+r that followed
each INSTALL_DATA, and the "-" that made the recursive one ignore failures;
INSTALL_DATA already sets the mode.

Also guard MV_DIRS with "test -e".  It is used to move the built subdirectories
into place, and with a DESTDIR install some of them are not there, so the
unguarded "mv" failed the whole target.

Also install the man pages under the name program_transform_name asks
for, so that more than one Emacs can share a prefix.

--- Makefile.in.orig
+++ Makefile.in
@@ -141,7 +141,7 @@
 infodir=@infodir@
 INFO_FILES=ada-mode auth autotype calc ccmode cl dbus dired-x ebrowse	\
            ede ediff edt eieio efaq eintr elisp emacs emacs-mime epa erc \
-	   eshell eudc flymake forms gnus idlwave info mairix-el	\
+	   eshell eudc flymake forms gnus idlwave mairix-el	\
 	   message mh-e newsticker nxml-mode org pcl-cvs pgg rcirc	\
 	   reftex remember sasl sc semantic ses sieve smtpmail speedbar \
 	   tramp url vip viper widget woman
@@ -403,7 +403,7 @@
 install: all install-arch-indep install-arch-dep install-leim blessmail
 	@true
 
-MV_DIRS = for i in $$dir; do rm -fr `basename "$$i"` ; mv "$$i" . ; done
+MV_DIRS = for i in $$dir; do if test -e $$i ; then rm -fr `basename "$$i"` ; mv "$$i" . ; fi; done
 
 ### Install the executables that were compiled specifically for this machine.
 ### It would be nice to do something for a parallel make
@@ -574,27 +574,18 @@
 	      test "$(MAKEINFO)" = "off" && ! test -e $$elt && continue; \
 	      for f in `ls $$elt $$elt-[1-9] $$elt-[1-9][0-9] 2>/dev/null`; do \
 	        ${INSTALL_DATA} $$f $(DESTDIR)${infodir}/$$f; \
-	        chmod a+r $(DESTDIR)${infodir}/$$f; \
 	      done; \
 	   done); \
 	else true; fi
 	-unset CDPATH; \
-	thisdir=`/bin/pwd`; \
-	if [ `(cd ${srcdir}/info && /bin/pwd)` != `(cd $(DESTDIR)${infodir} && /bin/pwd)` ]; \
-	then \
-	  for elt in $(INFO_FILES); do \
-	    test "$(MAKEINFO)" = "off" && ! test -e $$elt && continue; \
-	    (cd $${thisdir}; \
-	     ${INSTALL_INFO} --info-dir=$(DESTDIR)${infodir} $(DESTDIR)${infodir}/$$elt); \
-	  done; \
-	else true; fi
-	-chmod -R a+r $(DESTDIR)${datadir}/emacs/${version} ${COPYDESTS}
+	chmod -R a+r $(DESTDIR)${datadir}/emacs/${version} ${COPYDESTS}
 	thisdir=`/bin/pwd`; \
 	cd ${mansrcdir}; \
 	for page in ${MAN_PAGES}; do \
 	  (cd $${thisdir}; \
-	   ${INSTALL_DATA} ${mansrcdir}/$${page} $(DESTDIR)${man1dir}/$${page}; \
-	   chmod a+r $(DESTDIR)${man1dir}/$${page}); \
+	   name=`echo $${page} | sed 's/\.1$$//' | sed '$(TRANSFORM)'`.1; \
+	   ${INSTALL_DATA} ${mansrcdir}/$${page} $(DESTDIR)${man1dir}/$${name}; \
+	   chmod a+r $(DESTDIR)${man1dir}/$${name}); \
 	done
 
 ## Install those items from etc/ that need to end up elsewhere.
