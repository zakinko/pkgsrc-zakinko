--- devel/cqual/Makefile.orig
+++ devel/cqual/Makefile
@@ -12,6 +12,7 @@
 MAKE_JOBS_SAFE=		no
 USE_PKGLOCALEDIR=	yes
 GNU_CONFIGURE=		yes
+INSTALL_MAKE_FLAGS+=	lispdir=${EMACS_LISPPREFIX}/cqual
 USE_TOOLS+=		gmake perl:run bison m4
 
 REPLACE_PERL+=		bin/*
