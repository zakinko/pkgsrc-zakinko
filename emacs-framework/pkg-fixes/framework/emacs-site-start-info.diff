--- editors/emacs29/Makefile.common.orig
+++ editors/emacs29/Makefile.common
@@ -85,3 +85,15 @@
 	${CHMOD} -R go-w ${WRKSRC}
 
 .include "../../devel/libgetopt/buildlink3.mk"
+
+# The lisp of other packages lives under this version's directory and so
+# does their info, but Emacs builds Info-default-directory-list from its
+# own --infodir at configure time and cannot be told at run time except
+# through INFOPATH.  This adds the version's info directory to the list
+# at startup; without it the files land where nothing looks.
+post-install: emacs-info-path
+emacs-info-path:
+	${ECHO} '(add-to-list (quote Info-default-directory-list)' \
+		> ${DESTDIR}${PREFIX}/share/emacs/${PKGVERSION_NOREV}/site-lisp/site-start.el
+	${ECHO} '  (expand-file-name "../info" (file-name-directory load-file-name)) t)' \
+		>> ${DESTDIR}${PREFIX}/share/emacs/${PKGVERSION_NOREV}/site-lisp/site-start.el
--- editors/emacs29/PLIST.orig
+++ editors/emacs29/PLIST
@@ -4210,6 +4210,7 @@
 share/emacs/${PKGVERSION}/lisp/yank-media.el.gz
 share/emacs/${PKGVERSION}/lisp/yank-media.elc
 share/emacs/${PKGVERSION}/site-lisp/subdirs.el
+share/emacs/${PKGVERSION}/site-lisp/site-start.el
 share/emacs/site-lisp/subdirs.el
 share/icons/hicolor/128x128/apps/emacs.png
 share/icons/hicolor/16x16/apps/emacs.png
--- editors/emacs30/Makefile.common.orig
+++ editors/emacs30/Makefile.common
@@ -89,3 +89,15 @@
 .if ${OPSYS} != "Darwin"
 .  include "../../devel/libgetopt/buildlink3.mk"
 .endif
+
+# The lisp of other packages lives under this version's directory and so
+# does their info, but Emacs builds Info-default-directory-list from its
+# own --infodir at configure time and cannot be told at run time except
+# through INFOPATH.  This adds the version's info directory to the list
+# at startup; without it the files land where nothing looks.
+post-install: emacs-info-path
+emacs-info-path:
+	${ECHO} '(add-to-list (quote Info-default-directory-list)' \
+		> ${DESTDIR}${PREFIX}/share/emacs/${PKGVERSION_NOREV}/site-lisp/site-start.el
+	${ECHO} '  (expand-file-name "../info" (file-name-directory load-file-name)) t)' \
+		>> ${DESTDIR}${PREFIX}/share/emacs/${PKGVERSION_NOREV}/site-lisp/site-start.el
--- editors/emacs30/PLIST.orig
+++ editors/emacs30/PLIST
@@ -4360,6 +4360,7 @@
 share/emacs/${PKGVERSION}/lisp/yank-media.el.gz
 share/emacs/${PKGVERSION}/lisp/yank-media.elc
 share/emacs/${PKGVERSION}/site-lisp/subdirs.el
+share/emacs/${PKGVERSION}/site-lisp/site-start.el
 share/emacs/site-lisp/subdirs.el
 share/icons/hicolor/128x128/apps/emacs.png
 share/icons/hicolor/16x16/apps/emacs.png
--- editors/emacs31/Makefile.common.orig
+++ editors/emacs31/Makefile.common
@@ -89,3 +89,15 @@
 .if ${OPSYS} != "Darwin"
 .  include "../../devel/libgetopt/buildlink3.mk"
 .endif
+
+# The lisp of other packages lives under this version's directory and so
+# does their info, but Emacs builds Info-default-directory-list from its
+# own --infodir at configure time and cannot be told at run time except
+# through INFOPATH.  This adds the version's info directory to the list
+# at startup; without it the files land where nothing looks.
+post-install: emacs-info-path
+emacs-info-path:
+	${ECHO} '(add-to-list (quote Info-default-directory-list)' \
+		> ${DESTDIR}${PREFIX}/share/emacs/${PKGVERSION_NOREV}/site-lisp/site-start.el
+	${ECHO} '  (expand-file-name "../info" (file-name-directory load-file-name)) t)' \
+		>> ${DESTDIR}${PREFIX}/share/emacs/${PKGVERSION_NOREV}/site-lisp/site-start.el
--- editors/emacs31/PLIST.orig
+++ editors/emacs31/PLIST
@@ -4428,6 +4428,7 @@
 share/emacs/${PKGVERSION}/lisp/yank-media.el.gz
 share/emacs/${PKGVERSION}/lisp/yank-media.elc
 share/emacs/${PKGVERSION}/site-lisp/subdirs.el
+share/emacs/${PKGVERSION}/site-lisp/site-start.el
 share/emacs/site-lisp/subdirs.el
 share/icons/hicolor/128x128/apps/emacs.png
 share/icons/hicolor/16x16/apps/emacs.png
--- editors/emacs20/Makefile.orig
+++ editors/emacs20/Makefile
@@ -111,3 +111,15 @@
 	${CHOWN} -R ${SHAREOWN}:${SHAREGRP} ${DESTDIR}${PREFIX}/share/emacs
 
 .include "../../mk/bsd.pkg.mk"
+
+# The lisp of other packages lives under this version's directory and so
+# does their info, but Emacs builds Info-default-directory-list from its
+# own --infodir at configure time and cannot be told at run time except
+# through INFOPATH.  This adds the version's info directory to the list
+# at startup; without it the files land where nothing looks.
+post-install: emacs-info-path
+emacs-info-path:
+	${ECHO} '(add-to-list (quote Info-default-directory-list)' \
+		> ${DESTDIR}${PREFIX}/share/emacs/${PKGVERSION_NOREV}/site-lisp/site-start.el
+	${ECHO} '  (expand-file-name "../info" (file-name-directory load-file-name)) t)' \
+		>> ${DESTDIR}${PREFIX}/share/emacs/${PKGVERSION_NOREV}/site-lisp/site-start.el
--- editors/emacs20/PLIST.orig
+++ editors/emacs20/PLIST
@@ -1275,5 +1275,6 @@
 share/emacs/${PKGVERSION}/lisp/zone-mode.el
 share/emacs/${PKGVERSION}/lisp/zone-mode.elc
 share/emacs/${PKGVERSION}/site-lisp/subdirs.el
+share/emacs/${PKGVERSION}/site-lisp/site-start.el
 share/emacs/site-lisp/subdirs.el
 @pkgdir share/emacs/${PKGVERSION}/leim
